# Backwards-compatible include for custom configurations that used zones.yaml.
# Official firmware variants use tracking-ld2450.yaml directly.
packages:
  tracking_ld2450: !include tracking-ld2450.yaml
